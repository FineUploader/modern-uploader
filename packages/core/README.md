# Core plug-in

The core plug-in provides the most basic low-level API methods and events necessary to support other higher-level plug-ins.
This includes support for events, tracking and manipulating generic items (`File`/`Blob`, etc), and loading other plug-ins.
